/// <reference path="./../types.ts" />
import {CreateController} from './create.controller';
import {CreateFactory} from './create.factory';

/*global describe, it, beforeEach, inject, expect*/
describe('CreateController Tests', () => {

  it('null is not the same as undefined', () => expect(null).not.toEqual(undefined));

});

describe('CreateFactory Tests', () => {

  // it ('Should produce an empty TA bean',
    // () => let ta = new CreateFactory.EmptyTaBeanService(),
     // console.dir(ta);
     // );


});
