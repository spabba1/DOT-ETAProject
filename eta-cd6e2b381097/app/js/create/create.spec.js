System.register([], function(exports_1, context_1) {
    "use strict";
    var __moduleName = context_1 && context_1.id;
    return {
        setters:[],
        execute: function() {
            /*global describe, it, beforeEach, inject, expect*/
            describe('CreateController Tests', function () {
                it('null is not the same as undefined', function () { return expect(null).not.toEqual(undefined); });
            });
            describe('CreateFactory Tests', function () {
                // it ('Should produce an empty TA bean',
                // () => let ta = new CreateFactory.EmptyTaBeanService(),
                // console.dir(ta);
                // );
            });
        }
    }
});
//# sourceMappingURL=create.spec.js.map