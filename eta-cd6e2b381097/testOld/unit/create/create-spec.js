//ta-create-spec.js
/*global describe, it, beforeEach, inject, expect*/
describe('TA Create', function () {
    //'use strict';

    //beforeEach(module('eta'));

    //it('should return an empty TA Bean', inject(function ($controller) {
        //var scope = {},
            //ctrl;
        //scope.ta = {};
        //scope.ta.formtype = "create";

        //$controller('CreateController', {$scope: scope});

        //expect(scope.ta.formtype).toBe('create');
    //}));

    //it('should be true', function () {
        //expect(true).toBe(true);
    //});
});
